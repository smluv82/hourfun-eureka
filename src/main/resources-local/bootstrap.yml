#SERVER CONFIGURATION
server:
  port: ${PORT:6002}
  servlet:
    application-display-name: hourfun-eureka

#SPRING CONFIGURATION
spring:
  application:
    name: hourfun-eureka
  profiles:
    active: local
  cloud:
    config:
      #http://localhost:6999/hourfun-eureka/local
      uri: http://localhost:6999
      # name: hourfun-eureka